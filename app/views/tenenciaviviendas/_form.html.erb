<div class="ui container">
<%= form_for(tenenciavivienda) do |f| %>
  <% if tenenciavivienda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tenenciavivienda.errors.count, "error") %> prohibited this tenenciavivienda from being saved:</h2>

      <ul>
      <% tenenciavivienda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit "Crear",  class:"ui primary button btnLogin" %>
  </div>
<% end %>
</div>
