<div class="ui container">
<%= form_for(paise, html: {class: "ui form"}) do |f| %>
  <% if paise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(paise.errors.count, "error") %> prohibited this paise from being saved:</h2>

      <ul>
      <% paise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :bandera %>
    <%= f.text_field :bandera %>
  </div>

  <div class="actions">
    <%= f.submit "Crear",  class:"ui primary button btnLogin" %>
  </div>
<% end %>
</div>
