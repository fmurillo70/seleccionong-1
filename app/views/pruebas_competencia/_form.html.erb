<%= form_for(pruebas_competencium) do |f| %>
  <% if pruebas_competencium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pruebas_competencium.errors.count, "error") %> prohibited this pruebas_competencium from being saved:</h2>

      <ul>
      <% pruebas_competencium.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
