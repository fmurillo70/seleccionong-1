<p id="notice"><%= notice %></p>

<h1>Voluntario Pruebas</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @voluntario_pruebas.each do |voluntario_prueba| %>
      <tr>
        <td><%= link_to 'Show', voluntario_prueba %></td>
        <td><%= link_to 'Edit', edit_voluntario_prueba_path(voluntario_prueba) %></td>
        <td><%= link_to 'Destroy', voluntario_prueba, method: :delete, data: { confirm: 'Estas seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Voluntario Prueba', new_voluntario_prueba_path %>
