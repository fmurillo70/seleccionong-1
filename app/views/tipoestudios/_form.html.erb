<%= form_for(tipoestudio) do |f| %>
  <% if tipoestudio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipoestudio.errors.count, "error") %> prohibited this tipoestudio from being saved:</h2>

      <ul>
      <% tipoestudio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
