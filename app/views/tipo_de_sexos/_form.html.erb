<div class="ui container">

<%= form_for(tipo_de_sexo) do |f| %>
  <% if tipo_de_sexo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_de_sexo.errors.count, "error") %> prohibited this tipo_de_sexo from being saved:</h2>

      <ul>
      <% tipo_de_sexo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit "Crear",  class:"ui primary button btnLogin" %>
  </div>
<% end %>
</div>