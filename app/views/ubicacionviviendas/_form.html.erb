<%= form_for(ubicacionvivienda) do |f| %>
  <% if ubicacionvivienda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ubicacionvivienda.errors.count, "error") %> prohibited this ubicacionvivienda from being saved:</h2>

      <ul>
      <% ubicacionvivienda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
